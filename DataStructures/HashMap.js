// A HashMap is used to map keys to values. It uses a hash function to compute
// an index that maps to a value

function HashMap() {
  this.items = []
}

function computeHash(item) {
  // modulo
  return
}

/**
 * Insert
 * @param  {string} item
 * @return this
 */
HashMap.prototype.insert = function(item) {

}

/**
 * Search
 * @desc   Complexity: O(n)
 * @param  {string} item
 * @return this
 */
HashMap.prototype.search = function(item) {

}

/**
 * Delete
 * @desc   Complexity: O(n)
 * @param  {string} item
 * @return this
 */
HashMap.prototype.delete = function(item) {

}
