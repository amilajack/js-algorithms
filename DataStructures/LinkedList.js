import assert from 'assert'

function LinkedList() {
  this.links = []
}

LinkedList.prototype = {

  /**
   * @desc   Complexity: O(1)
   * @param  {string} item
   * @return this
   */
  insert(item) {

  },

  /**
   * Insert
   * @desc   Complexity: O(1)
   * @param  {string} item
   * @return this
   */
  get() {

  },

  /**
   * Insert
   * @desc   Complexity: O(1)
   * @param  {string} item
   * @return this
   */
  delete() {

  }
}
